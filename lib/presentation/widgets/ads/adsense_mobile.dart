import 'package:flutter/material.dart';

class WebAd extends StatelessWidget {
  final String type;
  const WebAd({super.key, required this.type});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 0);
  }
}
